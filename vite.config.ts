import { defineConfig } from "vite";

// Publicado en GitHub Pages bajo /connector-day-race/. Si más adelante se
// sirve desde un dominio propio (ej. vla.cr/carrera), cambiar base a "/".
export default defineConfig({
  base: "/connector-day-race/",
});
